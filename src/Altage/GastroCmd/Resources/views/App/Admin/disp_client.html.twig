{% extends 'AltageGastroCmd::app_layout.html.twig' %}
{% set client = clients %}

{% block title %}Gérer les clients  - {{ parent() }} {% endblock %}

{% block main_content %}
    
				<div class="page-content">
					<div class="page-header">
						<h1>
							Administration
							<small>
								<i class="ace-icon fa fa-angle-double-right"></i>
								Gérer les clients
							</small>
						</h1>
					</div><!-- /.page-header -->

					<div class="row">
						<div class="col-xs-12">
							<!-- PAGE CONTENT BEGINS -->
							<div class="row">
								<div class="col-xs-12">
									<table id="sample-table-1" class="table table-striped table-bordered table-hover">
										<thead>
											<tr>
												<th>Nom</th>
												<th>Pr&eacute;nom</th>
												<th>Date d'entrée</th>
												<th>Date de sortie</th>
                        <th>Textures</th>
                        <th>Actif</th>
												<th></th>
											</tr>
										</thead>

										<tbody>
                      {% for client in clients %}
											<tr>
												<td>{{ client.lastname }}</td>
												<td>{{ client.firstname }}</td>
												<td>{{ client.entryDate|date("d.m.Y") }}</td>
												<td>{{ client.leaveDate|date("d.m.Y") }}</td>
                        <td>
                        {% if client.texture|length > 0 %}
                            {% for textureClient in client.texture %}
                                {{ textureClient.nomTexture }}
                            {% endfor %}
                        {% endif %}
                        </td>
                        <td>{% if client.isActive %}
                                <i class="ace-icon fa fa-check green"></i>
                            {% else %}
                                <i class="ace-icon glyphicon glyphicon-remove red"></i>
                            {% endif %}
                        </td>
												<td>
													<div class="hidden-sm hidden-xs btn-group">
														<a href="{{ path('_AdminEditClient', { 'id': client.id } ) }}">
															<button class="btn btn-xs btn-info">
																<i class="ace-icon fa fa-pencil bigger-120"></i>
															</button>
														</a>														
													</div>
												</td>
											</tr>
                      {% endfor %}
										</tbody>
									</table>
								</div><!-- /.span -->
							</div><!-- /.row -->
            </div><!-- /.page-content -->
          </div><!-- /.main-content -->
        </div>
{% endblock %}